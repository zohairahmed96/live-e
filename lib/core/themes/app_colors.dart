import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const bgTop = Color(0xFF5CB4FF);
  static const bgBottom = Color(0xFF2D6DD6);
  static const white = Colors.white;
  static const textLight = Colors.white;
}
